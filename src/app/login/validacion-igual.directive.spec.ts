import { ValidacionIgualDirective } from './validacion-igual.directive';

describe('ValidacionIgualDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacionIgualDirective();
    expect(directive).toBeTruthy();
  });
});

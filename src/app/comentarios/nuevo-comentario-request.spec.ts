import { NuevoComentarioRequest } from './nuevo-comentario-request';

describe('NuevoComentarioRequest', () => {
  it('should create an instance', () => {
    expect(new NuevoComentarioRequest()).toBeTruthy();
  });
});
